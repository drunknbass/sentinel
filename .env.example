# Toggle optional geocoding; leave empty/false to skip geocoding for zero-cost
GEOCODE_ENABLED=false

# Vercel KV for edge-cached geocoding (RECOMMENDED for Vercel deployments)
# Automatically configured when you create a Vercel KV database
# See VERCEL_KV_SETUP.md for complete setup instructions
# Free tier: 256MB storage, 100k reads/month, 1k writes/month
# These variables are auto-populated by Vercel - no manual setup needed:
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Apple MapKit JS credentials for maps (client-side only)
# Create key at: https://developer.apple.com/account/resources/authkeys/add
# Enable "MapKit JS" service for interactive maps
# Team ID: Find at https://developer.apple.com/account/#!/membership/
# APPLE_MAPKIT_TEAM_ID=
# APPLE_MAPKIT_KEY_ID=
# APPLE_MAPKIT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
# ...your private key here...
# -----END PRIVATE KEY-----"

# Optional override of Census endpoint (fallback geocoding)
CENSUS_GEOCODER_BASE=https://geocoding.geo.census.gov/geocoder/locations/onelineaddress

# Cache TTL seconds for scrape + geocode
CACHE_TTL_SECONDS=60
GEOCODE_CACHE_TTL_SECONDS=259200

# Optional MapBox token for MapBox tiles (leave empty to use free CartoDB tiles)
# Get your token at https://account.mapbox.com/access-tokens/
# NEXT_PUBLIC_MAPBOX_TOKEN=
